@GenModel(basePackage="org.vaultage.demo.fairnet")
@namespace(uri="fairnet", prefix="")
package fairnet;


@vault
class FairnetVault {
	
	attr String name;
	val Friend[*] friends;
	val Post[*] posts;

	@vaultage.operation(async="true")
 	op Boolean addFriend();
	
	op Post getPost(String postId);
	op String[*] getPosts();
	
	@public 
	op String getName();
}

class Friend {
	attr String name;
	attr String publicKey;
}

class Post {
	attr String content;
	attr boolean isPublic;
}

